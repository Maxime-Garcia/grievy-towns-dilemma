# Handoff — 2026-07-10 — Grievy Town's Dilemma

Note de reprise écrite en fin de session (travail sur poste professionnel, sans
`gh` CLI disponible — les PR n'ont pas pu être créées automatiquement).
Trois chantiers en parallèle, statuts différents. Lire ce fichier + les liens
avant de reprendre.

---

## 1. Tri d'inventaire par catégorie — ✅ TERMINÉ, déjà mergé

Branche `feat/inventory-category-sort` mergée dans `master` (fast-forward),
branche supprimée. Rien à faire. Regroupement par catégorie (armes/armures/
accessoires/consommables/matériaux/objets-clés/skins), trié par rareté
croissante à l'intérieur de chaque groupe, bandeaux d'en-tête qui défilent
avec la grille.

---

## 2. Arsenal/Bestiaire (lore, cross-link, transitions) — 🟡 PRÊT, PR à ouvrir

**Branche `feat/arsenal-bestiary-links`, poussée sur origin.**
Créer la PR : https://github.com/Maxime-Garcia/grievy-towns-dilemma/pull/new/feat/arsenal-bestiary-links

Contenu :
- Lore enrichi sur ~131 équipements de l'Arsenal (2-5 phrases par item,
  calibré par rareté, cohérent avec les divinités/zones déjà établies).
- Passif d'un item affiché en gras (or) dans l'encadré stats, plus dans le
  lore.
- Cross-link bidirectionnel Arsenal ↔ Bestiaire avec popup de confirmation
  Aller (bleu/cyan)/Annuler.
- Transitions modernisées : ouverture = matérialisation (fade+scale-in),
  fermeture = dissolution, redirection croisée = portail arcane (anneau qui
  se referme/rouvre, teinte de destination) — généralisées à TOUS les menus
  qui s'ouvrent (Inventaire/Pause/Shop/Skill/Dialogue), pas seulement
  Arsenal/Bestiaire.
- Bouton `[DEBUG] Tout débloquer` dans le Bestiaire.
- Debug touche G corrigé : donne maintenant armes+armures+accessoires (pas
  seulement les armes) et marque tout comme découvert dans l'Arsenal.
- Deux bugs de fond corrigés en cours de route : écran figé après fermeture/
  redirection (mutation du scene manager depuis un callback caméra non
  différée), crash `TypeError` dans `shutdown()` (`cameras.main` déjà
  undefined à ce point du lifecycle Phaser).

**Statut qualité** : 0 BLOCKER/BUG au code-review (2 passes). Testé en jeu par
Maxime, transitions validées, ajustements demandés déjà appliqués.

**Reste à faire** : ouvrir la PR (lien ci-dessus), puis merger quand validé.

---

## 3. Stats aléatoires façon Dofus (équipement) — 🟠 EN COURS

**Branche `feat/loot-stat-rolls`, poussée sur origin** (dernier commit
`bb5c3d9` au moment de cette note — mise à jour après une interruption
volontaire, voir section WIP ci-dessous).
PR (à ouvrir plus tard, pas encore prête) : https://github.com/Maxime-Garcia/grievy-towns-dilemma/pull/new/feat/loot-stat-rolls

Design complet dans `docs/design/LOOT_STAT_ROLLS.md` (déjà lu/validé) :
fourchettes de stats par item, algorithme de "Résonance" (qualité globale +
jitter par ligne), nombre de lignes par rareté (Common 2 → Hidden 8), 6
nouvelles stats (MDEF_FLAT, CDR_PCT, DODGE_PCT, BOSS_DMG_PCT, HP/MANA_ON_KILL),
migration de save invisible en puissance, mannequins de test.

### Fait et review (0 BLOCKER/BUG sur 2 passes de code-review indépendantes) :
- **Moteur** : types, `StatRollSystem` (rollItem/computeQuality/
  finalizeCatalogue), branchements StatsSystem/CombatSystem/SkillSystem,
  tous les points d'acquisition (loot ennemi, boutique, récompense de quête,
  lootables de terrain, drop garanti de boss), migration de save
  (`SAVE_VERSION` 1.6.0 → 1.7.0).
- **Contenu** : `equipRanges` sur les 135 équipements du jeu (conversion des
  ~80 items qui avaient des stats fixes + authoring complet des ~40 qui
  n'avaient AUCUNE stat — bug de data corrigé au passage — + les 9 items
  HIDDEN à 7 substats/budget contenu).
- **Monstres de test** : "Mannequins de Kelvar" (`training_dummy_straw`/
  `training_dummy_gilded`, 1 PV, drop garanti 100% Legendary/Mythic à
  fourchette large), gatés derrière `player.flags['dev_training_dummies']`.
  **Touche T en jeu** pour activer le flag (ajouté après coup — sans point
  d'entrée le flag n'était jamais posé), puis changer de zone pour les faire
  apparaître (cour de la caserne de Grievy Town, à côté de Kelvar).

**➡️ Le mécanisme central est testable dès maintenant** : les écrans
existants (Arsenal/Inventaire) affichent déjà `equipStats`, qui contient
maintenant les valeurs RÉELLEMENT ROLLÉES au lieu des valeurs catalogue
fixes — pas besoin d'attendre la passe UI ci-dessous pour vérifier que
l'aléatoire fonctionne (tuer les mannequins plusieurs fois, comparer les
instances dans le sac).

### 🔴 WIP INTERROMPU — commit `bb5c3d9`, NON REVIEW, NE PAS MERGER TEL QUEL

Une passe UI (§11 du design doc) avait été lancée (dev-agent) pour :
1. Corriger un bug d'affichage déjà identifié : le panneau détail de
   l'Inventaire (`renderItemDetail`/`showActionConfirmPopup`) lit
   `ALL_ITEMS[itemId]` (le catalogue) au lieu de l'instance réelle du slot —
   donc affiche les valeurs centre au lieu des valeurs rollées. Fix : faire
   remonter l'instance réelle (pas juste l'id) à travers toute la chaîne
   d'appel depuis le clic sur la grille.
2. ArsenalScene (vue catalogue) : afficher les fourchettes possibles
   (`PV 91–150`) au lieu des valeurs figées actuelles.
3. InventoryScene (vue instance) : valeur rollée en gras/doré + fourchette en
   petit gris + Résonance globale en % avec libellé coloré (Sourde/Stable/
   Claire/Vibrante/Parfaite) + teinte par ligne selon sa qualité locale.

**L'agent a été arrêté manuellement en cours de route** (contrainte de temps
— Maxime devait éteindre son poste). Son travail partiel sur
`src/scenes/ArsenalScene.ts`, `src/scenes/InventoryScene.ts` et
`src/utils/UITheme.ts` a quand même été **committé en WIP** (commit
`bb5c3d9`, poussé sur origin) plutôt que perdu :
- `npx tsc --noEmit` passe (0 erreur) sur cet état.
- **Mais rien n'a été relu, testé en jeu, ni passé au code-reviewer.** On ne
  sait pas si les 3 tâches ci-dessus sont complètes, partielles, ou juste
  esquissées — l'agent a été coupé avant son rapport final (dernière action
  connue : il vérifiait la présence de `tsc` pour lancer sa propre
  vérification, donc probablement proche de la fin mais pas confirmé).

**À faire en priorité en reprenant** : relire le diff de `bb5c3d9` en entier
avant toute chose (comparer à la description des 3 tâches ci-dessus + à
`docs/design/LOOT_STAT_ROLLS.md` §7/§4.3) pour évaluer ce qui est fait/à
finir/à corriger. Ne pas supposer que c'est terminé juste parce que ça
compile.

### Reste à faire après validation/finition de la passe UI
1. `gamefeel-agent` : notification de drop enrichie pour les rolls
   Vibrante/Parfaite (scintillement, mention dans la notif — §7.2 du design
   doc).
2. `code-reviewer` final sur toute la branche.
3. Mise à jour `BUILD_LABEL`, commit final, push, ouverture PR, merge après
   validation.

---

## Pour reprendre (depuis n'importe quelle machine)

```bash
git fetch origin
git checkout feat/loot-stat-rolls   # ou feat/arsenal-bestiary-links selon le chantier
git log --oneline -5                # vérifier qu'on est bien reparti du dernier commit connu
```

Puis demander à Claude Code de reprendre à l'endroit indiqué ci-dessus. Ce
fichier peut être supprimé une fois les 3 chantiers mergés dans `master`.
